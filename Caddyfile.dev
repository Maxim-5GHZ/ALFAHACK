{
    admin off
    auto_https off
}

:80 {

    log {
        output file /data/caddy/logs/access.log
    }

    @backend {
        path /api/*
        path /health
    }
    reverse_proxy @backend backend:8000

    reverse_proxy frontend:3000
}
